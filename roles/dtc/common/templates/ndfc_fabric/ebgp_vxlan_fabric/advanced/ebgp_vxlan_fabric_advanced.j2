{# Auto-generated NDFC eBGP VXLAN EVPN Advanced config data structure for fabric {{ vxlan.fabric.name }} #}
  OVERLAY_MODE: cli
  GRFIELD_DEBUG_FLAG: Enable
  ENABLE_PVLAN: false
  AAA_REMOTE_IP_ENABLED: False
  FABRIC_MTU: {{ vxlan.underlay.general.intra_fabric_interface_mtu | default(defaults.vxlan.underlay.general.intra_fabric_interface_mtu) }}
  L2_HOST_INTF_MTU: {{ vxlan.underlay.general.layer2_host_interface_mtu | default(defaults.vxlan.underlay.general.layer2_host_interface_mtu) }}
{% if vxlan.routed.bootstrap is defined and vxlan.routed.bootstrap.enable_cdp_mgmt is defined %}
  CDP_ENABLE: {{ vxlan.routed.bootstrap.enable_cdp_mgmt }}
{% endif %}
  ENABLE_NXAPI: {{ vxlan.routed.enable_nxapi_https | default(defaults.vxlan.routed.enable_nxapi_https) }}
{% if vxlan.routed.enable_nxapi_https | default(defaults.vxlan.routed.enable_nxapi_https) | ansible.builtin.bool %}
  NXAPI_HTTPS_PORT: {{ vxlan.routed.nxapi_https_port | default(defaults.vxlan.routed.nxapi_https_port) }}
  ENABLE_NXAPI_HTTP: {{ vxlan.routed.enable_nxapi_http | default(defaults.vxlan.routed.enable_nxapi_http) }}
{% if vxlan.routed.enable_nxapi_http | default(defaults.vxlan.routed.enable_nxapi_http) | ansible.builtin.bool %}
  NXAPI_HTTP_PORT: {{ vxlan.routed.nxapi_http_port | default(defaults.vxlan.routed.nxapi_http_port) }}
{% endif %}
{% endif %}
  SNMP_SERVER_HOST_TRAP: {{ vxlan.routed.snmp_server_host_trap | default(defaults.vxlan.routed.snmp_server_host_trap) }}
{% if not (vxlan.underlay.general.enable_ipv6_underlay | default(defaults.vxlan.underlay.general.enable_ipv6_underlay) | ansible.builtin.bool) %}
  FEATURE_PTP: {{ vxlan.routed.ptp.enable | default(defaults.vxlan.routed.ptp.enable) }}
{% if vxlan.routed.ptp.enable is defined and vxlan.routed.ptp.enable | ansible.builtin.bool %}
  PTP_DOMAIN_ID: {{ vxlan.routed.ptp.domain_id | default(defaults.vxlan.routed.ptp.domain_id) }}
  PTP_LB_ID: {{ vxlan.routed.ptp.lb_id | default(defaults.vxlan.routed.ptp.lb_id) }}
{% if vxlan.routed.ptp.vlan_id is defined %}
  PTP_VLAN_ID: {{ vxlan.routed.ptp.vlan_id }}
{% endif %}
{% endif %}
{% endif %}
